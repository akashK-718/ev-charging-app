/**
 * /bookings — driver's booking history.
 * TODO (Milestone 3): list past + upcoming bookings with status badges.
 */
export default function BookingsPage() {
  return (
    <main className="min-h-screen px-6 py-12">
      <h1 className="font-display font-extrabold text-3xl">Your bookings</h1>
      <p className="mt-2 text-muted">Coming in Milestone 3.</p>
    </main>
  );
}
