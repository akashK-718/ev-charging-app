/**
 * /bookings/new — create a booking.
 * TODO (Milestone 3): slot picker, kWh estimate, cost summary.
 */
export default function NewBookingPage() {
  return (
    <main className="min-h-screen px-6 py-12">
      <h1 className="font-display font-extrabold text-3xl">Book a slot</h1>
      <p className="mt-2 text-muted">Booking flow coming in Milestone 3.</p>
    </main>
  );
}
